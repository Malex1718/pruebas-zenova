export default function CategoriesList() {
  // Seria realizar las mismas funciones que el archivo AddProducts.js
  // Cambiando los campos del formulario por los campos de categorias

  return (
    <div>
      <h1>Lista de categorias, quedo en proceso</h1>
    </div>
  );
}
